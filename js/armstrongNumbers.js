let array = []
for (let i = 0; i < 1000; i++) {
    array.push(i)
}


let findArmstrongNumbers = function(array) {
    let ans = []
    for (let j = 0; j < array.length; j++) {
        let digits = array[j].toString().length
        let sum = 0
        // let digits = 3
        // console.log(digits)
        // let thresevenone = 371
        // let number1 = String(thresevenone).split("")
        for (let k= 0; k < .length; k++) {
            num
        }

        // let number = number1.reduce((a,b) => a**digits+b**digits)
        // console.log(number,digits)
            if (number == array[j]) {
                ans.push(array[j])
            }
        }
        // console.log(ans)        
    }
    
    

findArmstrongNumbers(array)
  


